#include<vector>
#include <stdio.h>
/*
struct {
    std::vector<double> nums;
} mystruct;

int main() {
    int n;
    printf("Enter how many numbers you want to write: \n");
    scanf("%d", &n);
    if (n > 2) {
        printf("Enter numbers: \n");
        for (int i = 0; i < n; ++i) {
            double n1;
            scanf("%lf", &n1);
            mystruct.nums.push_back(n1);
        }
        printf("Your numbers: \n");
        for (int i = 0; i < n; ++i) {
            printf("%lf ", mystruct.nums[i]);
        }
        printf("\n");
        printf("sums: \n");
        size_t sizenums = mystruct.nums.size();
        if (sizenums % 2 == 0) {
            for (int i = 0; i < sizenums; ++i) {
                double sumcurr = mystruct.nums[i] + mystruct.nums[sizenums - i - 1];
                printf("%lf + %lf = %lf\n", mystruct.nums[i], mystruct.nums[sizenums - i - 1], sumcurr);
            }
        }
    } else {
        printf("Amount of numbers must be more than 2");
    }
    return 0;
}
*/
